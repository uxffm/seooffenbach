---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
	title="SEO Agentur Offenbach - Professionelle Suchmaschinenoptimierung"
	description="Professionelle SEO Agentur in Offenbach ✓ Mehr Sichtbarkeit bei Google ✓ Höhere Rankings ✓ Mehr Kunden durch lokale Suchmaschinenoptimierung"
>
	<!-- Hero Section -->
	<section class="hero">
		<div class="container">
			<div class="hero-content">
				<h1 class="hero-title">
					<span class="title-line title-line-1">
						⚡ SEO marketing
					</span>
					<span class="title-line title-line-2">
						direkt aus Offenbach 📎
					</span>
					<div class="sticker-container">
						<span class="sticker-3d sticker-mint">SEO Agentur</span>
						<span class="sticker-3d sticker-lime">Offenbach</span>
					</div>
				</h1>

				<div class="hero-footer">
					<a href="#kontakt" class="cta-button">
						<span>Kontaktieren</span>
						<span class="pushpin">📌</span>
					</a>
					<p class="hero-subtext">jetzt unser projekt besprechen</p>
				</div>
			</div>
		</div>
	</section>

	<!-- Services Grid Section -->
	<section class="services-grid-section" id="leistungen">
		<div class="container">
			<h2 class="section-title-new">Unsere Leistungen</h2>
			<div class="services-grid-new">
				<div class="service-card-new">
					<div class="card-number">01.</div>
					<h3 class="card-title">SEO BERATUNG &<br>STRATEGIE</h3>
					<p class="card-desc">Individuelle SEO-Strategien basierend auf einer detaillierten Analyse Ihrer Website und Ihrer Zielgruppe. Wir entwickeln einen maßgeschneiderten Plan für nachhaltigen Erfolg.</p>
					<span class="sticker-accent sticker-lime-accent">Strategie</span>
				</div>

				<div class="service-card-new">
					<div class="card-number">02.</div>
					<h3 class="card-title">TECHNISCHES<br>SEO</h3>
					<p class="card-desc">Optimierung der technischen Grundlagen: Ladezeiten, Mobile-Optimierung, Indexierung, strukturierte Daten und Core Web Vitals für bessere Rankings.</p>
					<span class="sticker-accent sticker-mint-accent">Technical</span>
				</div>

				<div class="service-card-new">
					<div class="card-number">03.</div>
					<h3 class="card-title">CONTENT-<br>OPTIMIERUNG</h3>
					<p class="card-desc">Suchmaschinenoptimierte Inhalte, die Ihre Zielgruppe ansprechen. Keyword-Recherche, Content-Erstellung und Optimierung bestehender Texte.</p>
					<span class="sticker-accent sticker-lime-accent">Content</span>
				</div>

				<div class="service-card-new">
					<div class="card-number">04.</div>
					<h3 class="card-title">LOCAL SEO<br>OFFENBACH</h3>
					<p class="card-desc">Spezialisierte Optimierung für lokale Suchanfragen. Google My Business, lokale Citations und Strategien für mehr Kunden aus Offenbach und Umgebung.</p>
					<span class="sticker-accent sticker-mint-accent">Local SEO</span>
				</div>

				<div class="service-card-new">
					<div class="card-number">05.</div>
					<h3 class="card-title">LINK<br>BUILDING</h3>
					<p class="card-desc">Aufbau hochwertiger Backlinks für mehr Autorität und bessere Rankings. Nachhaltiger Linkaufbau nach Google-Richtlinien.</p>
					<span class="sticker-accent sticker-lime-accent">Links</span>
				</div>

				<div class="service-card-new">
					<div class="card-number">06.</div>
					<h3 class="card-title">SEO-MONITORING<br>& REPORTING</h3>
					<p class="card-desc">Kontinuierliche Überwachung Ihrer Rankings und Performance. Transparente Reports mit allen wichtigen Kennzahlen und Entwicklungen.</p>
					<span class="sticker-accent sticker-mint-accent">Monitoring</span>
				</div>
			</div>
		</div>
	</section>

	<!-- Was wir machen -->
	<section class="about">
		<div class="container">
			<h2 class="section-title">Was wir machen</h2>
			<div class="about-grid">
				<div class="about-card card">
					<p>
						Als spezialisierte SEO-Agentur in Offenbach helfen wir Unternehmen dabei, ihre Online-Sichtbarkeit
						zu maximieren und nachhaltig mehr Kunden über Google zu gewinnen.
					</p>
					<p>
						Unsere Expertise liegt in der ganzheitlichen Suchmaschinenoptimierung – von der technischen
						Optimierung über Content-Strategien bis hin zu lokalem SEO für Offenbach und die Rhein-Main-Region.
					</p>
				</div>
			</div>
		</div>
	</section>

	<!-- Warum SEO -->
	<section class="why-seo">
		<div class="container">
			<h2 class="section-title">Warum Suchmaschinenoptimierung?</h2>
			<div class="why-grid">
				<div class="why-card card">
					<h3>Mehr Sichtbarkeit</h3>
					<p>90% der Online-Erfahrungen beginnen mit einer Suchmaschine. Seien Sie sichtbar, wenn Ihre Kunden suchen.</p>
				</div>
				<div class="why-card card">
					<h3>Qualifizierter Traffic</h3>
					<p>Erreichen Sie Nutzer mit konkreter Kaufabsicht genau zum richtigen Zeitpunkt.</p>
				</div>
				<div class="why-card card">
					<h3>Nachhaltige Ergebnisse</h3>
					<p>Im Gegensatz zu Werbeanzeigen bringen SEO-Maßnahmen langfristige und nachhaltige Erfolge.</p>
				</div>
				<div class="why-card card">
					<h3>Messbare Erfolge</h3>
					<p>Jede Maßnahme ist nachvollziehbar. Wir zeigen Ihnen transparent, was SEO für Ihr Geschäft bewirkt.</p>
				</div>
			</div>
		</div>
	</section>

	<!-- Blog Preview -->
	<section class="blog-preview">
		<div class="container">
			<h2 class="section-title">Aktuelles aus unserem Blog</h2>
			<p class="section-intro">Tipps, Trends und Best Practices rund um SEO</p>
			<div class="blog-cta">
				<a href="/blog" class="neon-button">Zum Blog →</a>
			</div>
		</div>
	</section>

	<!-- Kontakt -->
	<section class="contact" id="kontakt">
		<div class="container">
			<div class="contact-card card-neon-green">
				<h2>Bereit für bessere Rankings?</h2>
				<p>
					Lassen Sie uns gemeinsam Ihre SEO-Strategie entwickeln. Kontaktieren Sie uns für
					eine kostenlose Erstberatung.
				</p>
				<div class="contact-details">
					<p><strong>📍</strong> Offenbach am Main</p>
					<p><strong>📧</strong> kontakt@seo-agentur-offenbach.de</p>
					<p><strong>📞</strong> +49 (0) XXX XXXXXXX</p>
				</div>
				<a href="#kontakt" class="dark-button">Jetzt Kontakt aufnehmen</a>
			</div>
		</div>
	</section>
</BaseLayout>

<style>
	/* Hero Section - Neo-Brutalism */
	.hero {
		min-height: 100vh;
		display: flex;
		align-items: center;
		position: relative;
		overflow: hidden;
		background: var(--dark-petrol);
		background-image:
			radial-gradient(circle at 2px 2px, rgba(255, 255, 255, 0.15) 1px, transparent 0);
		background-size: 40px 40px;
		padding: 8rem 0;
		margin-top: -80px;
		padding-top: calc(8rem + 80px);
	}

	.hero-content {
		text-align: center;
		max-width: 1400px;
		margin: 0 auto;
		position: relative;
		z-index: 2;
	}

	.hero-tag {
		font-family: var(--font-mono);
		font-size: 0.875rem;
		font-weight: 400;
		color: rgba(255, 255, 255, 0.9);
		margin-bottom: 2rem;
		letter-spacing: 0.05em;
	}

	.hero-title {
		font-family: var(--font-display);
		font-size: clamp(2.5rem, 8vw, 5.5rem);
		font-weight: 900;
		line-height: 0.95;
		margin-bottom: 2.5rem;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
	}

	.title-line {
		color: var(--white);
		font-style: italic;
		letter-spacing: -0.03em;
	}

	.sticker-container {
		display: flex;
		gap: 0;
		margin-top: 2rem;
		flex-wrap: wrap;
		justify-content: center;
	}

	.sticker-3d {
		display: inline-block;
		font-size: clamp(2rem, 7vw, 4.5rem);
		font-weight: 900;
		font-style: italic;
		padding: 0.3em 0.8em;
		position: relative;
		letter-spacing: -0.02em;
		transform-style: preserve-3d;
		transition: transform 0.3s ease;
		border-radius: 20px;
	}

	.sticker-mint {
		background: var(--neon-mint);
		color: #0b3d4e;
		transform: rotate(-5deg);
		box-shadow:
			8px 8px 0 rgba(0, 0, 0, 0.2),
			12px 12px 30px rgba(0, 255, 179, 0.4);
		border: 4px solid rgba(0, 0, 0, 0.1);
	}

	.sticker-lime {
		background: var(--neon-lime);
		color: #0b3d4e;
		transform: rotate(3deg);
		box-shadow:
			8px 8px 0 rgba(0, 0, 0, 0.2),
			12px 12px 30px rgba(204, 255, 0, 0.4);
		border: 4px solid rgba(0, 0, 0, 0.1);
		margin-left: -3rem;
	}

	.sticker-3d:hover {
		transform: rotate(0deg) translateY(-5px);
	}

	/* Doodles and Accents */
	.doodles {
		position: absolute;
		width: 100%;
		height: 100%;
		top: 0;
		left: 0;
		pointer-events: none;
		z-index: 1;
	}

	.doodle {
		position: absolute;
		width: 100px;
		height: 100px;
	}

	.arrow-1 {
		top: 20%;
		left: 10%;
		transform: rotate(-20deg);
		animation: float 3s ease-in-out infinite;
	}

	.circle-1 {
		top: 60%;
		right: 15%;
		transform: rotate(15deg);
		animation: float 4s ease-in-out infinite;
	}

	.lightning-icon {
		position: absolute;
		font-size: 4rem;
		top: 30%;
		right: 20%;
		animation: pulse 2s ease-in-out infinite;
		filter: drop-shadow(0 0 20px var(--neon-yellow));
	}

	.paperclip {
		position: absolute;
		font-size: 3rem;
		bottom: 25%;
		left: 15%;
		transform: rotate(-45deg);
		animation: swing 3s ease-in-out infinite;
	}

	@keyframes float {
		0%, 100% { transform: translateY(0) rotate(-20deg); }
		50% { transform: translateY(-20px) rotate(-20deg); }
	}

	@keyframes pulse {
		0%, 100% { opacity: 0.7; transform: scale(1); }
		50% { opacity: 1; transform: scale(1.2); }
	}

	@keyframes swing {
		0%, 100% { transform: rotate(-45deg); }
		50% { transform: rotate(-35deg); }
	}

	/* Hero Footer */
	.hero-footer {
		display: flex;
		flex-direction: column;
		align-items: center;
		margin-top: 1.5rem;
	}

	.cta-button {
		background: var(--white);
		color: #000;
		padding: 1.25rem 3rem;
		border-radius: 100px;
		text-decoration: none;
		font-family: var(--font-display);
		font-weight: 800;
		font-size: 1.125rem;
		display: inline-flex;
		align-items: center;
		gap: 0.875rem;
		transition: all 0.3s ease;
		box-shadow:
			6px 6px 0 rgba(0, 0, 0, 0.2),
			0 10px 40px rgba(255, 255, 255, 0.2);
		border: 3px solid #000;
		position: relative;
	}

	.cta-button:hover {
		transform: translate(-3px, -3px);
		box-shadow:
			9px 9px 0 rgba(0, 0, 0, 0.2),
			0 15px 50px rgba(255, 255, 255, 0.3);
	}

	.pushpin {
		font-size: 1.5rem;
		transform: rotate(45deg);
	}

	.hero-subtext {
		margin-top: 1.5rem;
		color: rgba(255, 255, 255, 0.85);
		font-family: var(--font-mono);
		font-size: 0.875rem;
		letter-spacing: 0.05em;
		text-transform: lowercase;
	}

	/* Services Grid Section */
	.services-grid-section {
		padding: 6rem 0;
		background: #ffffff;
		background-image: radial-gradient(circle at 2px 2px, rgba(0, 0, 0, 0.08) 1px, transparent 0);
		background-size: 32px 32px;
		position: relative;
	}

	.section-title-new {
		font-family: var(--font-display);
		font-size: 3.5rem;
		font-weight: 900;
		text-align: center;
		margin-bottom: 4rem;
		color: #000;
		letter-spacing: -0.02em;
	}

	.services-grid-new {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2.5rem;
		max-width: 1400px;
		margin: 0 auto;
	}

	.service-card-new {
		background: #ffffff;
		padding: 3rem;
		border: 2px solid #000;
		border-radius: 0;
		position: relative;
		transition: transform 0.3s ease;
	}

	.service-card-new:hover {
		transform: translateY(-8px);
	}

	.card-number {
		font-family: var(--font-display);
		font-size: 3rem;
		font-weight: 900;
		color: #000;
		line-height: 1;
		margin-bottom: 1rem;
	}

	.card-title {
		font-family: var(--font-display);
		font-size: 2rem;
		font-weight: 900;
		color: #000;
		line-height: 1.1;
		margin-bottom: 1.5rem;
		letter-spacing: -0.02em;
	}

	.card-desc {
		font-size: 1rem;
		line-height: 1.6;
		color: #333;
		margin-bottom: 2rem;
	}

	.sticker-accent {
		display: inline-block;
		padding: 0.5rem 1.5rem;
		font-family: var(--font-display);
		font-weight: 800;
		font-size: 0.875rem;
		color: #000;
		border: 2px solid #000;
		border-radius: 100px;
		position: absolute;
		bottom: -15px;
		right: 30px;
		letter-spacing: 0.05em;
		text-transform: uppercase;
	}

	.sticker-lime-accent {
		background: var(--neon-lime);
		transform: rotate(-3deg);
		box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.2);
	}

	.sticker-mint-accent {
		background: var(--neon-mint);
		transform: rotate(2deg);
		box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.2);
	}

	/* About */
	.about {
		padding: 5rem 0;
		background: #ffffff;
	}

	.about-grid {
		max-width: 900px;
		margin: 0 auto;
	}

	.about-card p {
		font-size: 1.125rem;
		line-height: 1.8;
		margin-bottom: 1.5rem;
		color: #333;
	}

	/* Services */
	.services {
		padding: 5rem 0;
		background: #f5f5f5;
	}

	.section-title {
		font-size: 3.5rem;
		text-align: center;
		margin-bottom: 4rem;
		font-weight: 800;
		color: #333;
	}

	.services-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
		gap: 2rem;
	}

	.service-card {
		transition: transform 0.3s, box-shadow 0.3s;
	}

	.service-card:hover {
		transform: translateY(-8px) rotate(1deg);
	}

	.service-icon {
		font-size: 3.5rem;
		margin-bottom: 1.25rem;
	}

	.service-card h3 {
		font-size: 1.5rem;
		margin-bottom: 1rem;
		color: #333;
	}

	.service-card p {
		line-height: 1.7;
		color: #666;
	}

	.card-neon-green h3,
	.card-neon-cyan h3,
	.card-neon-yellow h3 {
		color: #000000;
	}

	.card-neon-green p,
	.card-neon-cyan p,
	.card-neon-yellow p {
		color: #000000;
		opacity: 0.85;
	}

	/* Why SEO */
	.why-seo {
		padding: 5rem 0;
		background: #ffffff;
	}

	.why-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 2rem;
	}

	.why-card h3 {
		font-size: 1.5rem;
		margin-bottom: 1rem;
		color: #333;
	}

	.why-card p {
		color: #666;
		line-height: 1.7;
	}

	/* Blog Preview */
	.blog-preview {
		padding: 5rem 0;
		text-align: center;
		background: #f5f5f5;
	}

	.section-intro {
		font-size: 1.125rem;
		color: #666;
		margin-bottom: 3rem;
	}

	.neon-button {
		display: inline-block;
		background: var(--neon-yellow);
		color: var(--dark-bg);
		padding: 1rem 3rem;
		border-radius: 50px;
		text-decoration: none;
		font-weight: 700;
		font-size: 1.1rem;
		box-shadow: 0 0 30px rgba(223, 255, 0, 0.5);
		transition: all 0.3s;
	}

	.neon-button:hover {
		transform: translateY(-3px);
		box-shadow: 0 0 40px rgba(223, 255, 0, 0.7);
	}

	/* Contact */
	.contact {
		padding: 5rem 0 8rem;
		background: #ffffff;
	}

	.contact-card {
		padding: 4rem;
		text-align: center;
		max-width: 800px;
		margin: 0 auto;
		border-radius: 20px;
	}

	.contact-card h2 {
		font-size: 3rem;
		margin-bottom: 2rem;
		font-weight: 800;
		color: var(--dark-bg);
	}

	.contact-card p {
		font-size: 1.125rem;
		line-height: 1.7;
		margin-bottom: 2.5rem;
		color: var(--dark-bg);
	}

	.contact-details {
		margin: 2.5rem 0;
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.contact-details p {
		margin: 0;
		font-weight: 600;
		color: var(--dark-bg);
	}

	.dark-button {
		display: inline-block;
		background: var(--dark-bg);
		color: var(--neon-green);
		padding: 1.25rem 3rem;
		border-radius: 50px;
		text-decoration: none;
		font-weight: 700;
		font-size: 1.1rem;
		transition: all 0.3s;
		box-shadow: 0 5px 20px rgba(0, 0, 0, 0.3);
	}

	.dark-button:hover {
		transform: translateY(-3px);
		box-shadow: 0 8px 30px rgba(0, 0, 0, 0.4);
	}

	/* Responsive */
	@media (max-width: 1024px) {
		.hero {
			padding: 6rem 0;
		}

		.doodles .lightning-icon,
		.doodles .paperclip {
			display: none;
		}

		.services-grid-new {
			grid-template-columns: repeat(2, 1fr);
		}

		.section-title-new {
			font-size: 2.5rem;
		}
	}

	@media (max-width: 768px) {
		.hero {
			padding: 4rem 0;
			min-height: auto;
		}

		.hero-title {
			margin-top: 3rem;
		}

		.hero-tag {
			font-size: 0.875rem;
			margin-bottom: 2rem;
		}

		.sticker-container {
			gap: 1rem;
		}

		.sticker-3d {
			font-size: clamp(2rem, 8vw, 4rem);
		}

		.doodles .arrow-1,
		.doodles .circle-1 {
			display: none;
		}

		.hero-footer {
			margin-top: 2rem;
		}

		.cta-button {
			padding: 1.25rem 2.5rem;
			font-size: 1rem;
		}

		.section-title {
			font-size: 2.5rem;
		}

		.services-grid,
		.why-grid {
			grid-template-columns: 1fr;
		}

		.contact-card {
			padding: 2.5rem 2rem;
		}

		.contact-card h2 {
			font-size: 2rem;
		}

		.services-grid-new {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.service-card-new {
			padding: 2rem;
		}

		.card-number {
			font-size: 2rem;
		}

		.card-title {
			font-size: 1.5rem;
		}

		.sticker-accent {
			bottom: -12px;
			right: 20px;
			padding: 0.4rem 1rem;
			font-size: 0.75rem;
		}
	}
</style>
</BaseLayout>

---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('blog');
const latestPosts = allPosts
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 3);

function getCategoryEmoji(category: string): string {
	const emojiMap: Record<string, string> = {
		'KI & Technologie': '🤖',
		'Local SEO': '📍',
		'Technical SEO': '⚙️',
		'Content SEO': '✍️',
		'E-Commerce & SEO': '🛒'
	};
	return emojiMap[category] || '📝';
}
---

<BaseLayout
	title="SEO Agentur Offenbach - Professionelle Suchmaschinenoptimierung"
	description="Professionelle SEO Agentur in Offenbach ✓ Mehr Sichtbarkeit bei Google ✓ Höhere Rankings ✓ Mehr Kunden durch lokale Suchmaschinenoptimierung"
>
	<!-- Hero Section -->
	<section class="hero">
		<div class="container">
			<div class="hero-content">
				<h1 class="hero-title">
					<span class="title-line title-line-1">
						⚡ SEO Marketing -
					</span>
					<span class="title-line title-line-2">
						direkt aus Offenbach 📎
					</span>
					<div class="sticker-container">
						<span class="sticker-3d sticker-mint">SEO Agentur</span>
						<span class="sticker-3d sticker-lime">Offenbach</span>
					</div>
				</h1>

				<div class="hero-footer">
					<a href="#kontakt" class="cta-button">
						<span>Kontaktieren</span>
						<span class="pushpin">📌</span>
					</a>
					<p class="hero-subtext">jetzt unser projekt besprechen</p>
				</div>
			</div>
		</div>
	</section>

	<!-- Was wir machen -->
	<section class="about">
		<div class="container">
			<div class="about-content">
				<h2 class="about-title">Was wir machen:</h2>
				<p class="about-subtitle">
					Als spezialisierte SEO-Agentur in Offenbach helfen wir Unternehmen dabei, ihre Online-Sichtbarkeit
					zu maximieren und nachhaltig mehr Kunden über Google zu gewinnen.
				</p>
				<p class="about-description">
					Unsere Expertise liegt in der ganzheitlichen Suchmaschinenoptimierung – von der technischen
					Optimierung über Content-Strategien bis hin zu lokalem SEO für das gesamte Stadtgebiet Offenbach.
					Wir bieten unsere Dienstleistungen in allen Stadtteilen an: Innenstadt, Bieber, Bürgel, Rumpenheim,
					Kaiserlei, Lauterborn, Rosenhöhe, Tempelsee, Mathildenviertel und Waldheim. Ob Sie in der Berliner Straße,
					am Mainufer oder im Gewerbegebiet ansässig sind – wir entwickeln maßgeschneiderte SEO-Strategien, die auf
					Ihre individuellen Geschäftsziele abgestimmt sind und nachhaltige Ergebnisse in der gesamten Rhein-Main-Region liefern.
				</p>
			</div>
		</div>
	</section>

	<!-- Services Grid Section -->
	<section class="services-grid-section" id="leistungen">
		<div class="container">
			<h2 class="section-title-new">Unsere Leistungen</h2>
			<div class="services-grid-new">
				<div class="service-card-new">
					<div class="card-number">01.</div>
					<h3 class="card-title">SEO BERATUNG &<br>STRATEGIE</h3>
					<p class="card-desc">Individuelle SEO-Strategien basierend auf einer detaillierten Analyse Ihrer Website und Ihrer Zielgruppe. Wir entwickeln einen maßgeschneiderten Plan für nachhaltigen Erfolg.</p>
					<span class="sticker-accent sticker-lime-accent">Strategie</span>
				</div>

				<div class="service-card-new">
					<div class="card-number">02.</div>
					<h3 class="card-title">TECHNISCHES<br>SEO</h3>
					<p class="card-desc">Optimierung der technischen Grundlagen: Ladezeiten, Mobile-Optimierung, Indexierung, strukturierte Daten und Core Web Vitals für bessere Rankings.</p>
					<span class="sticker-accent sticker-mint-accent">Technical</span>
				</div>

				<div class="service-card-new">
					<div class="card-number">03.</div>
					<h3 class="card-title">CONTENT-<br>OPTIMIERUNG</h3>
					<p class="card-desc">Suchmaschinenoptimierte Inhalte, die Ihre Zielgruppe ansprechen. Keyword-Recherche, Content-Erstellung und Optimierung bestehender Texte.</p>
					<span class="sticker-accent sticker-lime-accent">Content</span>
				</div>

				<div class="service-card-new">
					<div class="card-number">04.</div>
					<h3 class="card-title">LOCAL SEO<br>OFFENBACH</h3>
					<p class="card-desc">Spezialisierte Optimierung für lokale Suchanfragen. Google My Business, lokale Citations und Strategien für mehr Kunden aus Offenbach und Umgebung.</p>
					<span class="sticker-accent sticker-mint-accent">Local SEO</span>
				</div>

				<div class="service-card-new">
					<div class="card-number">05.</div>
					<h3 class="card-title">LINK<br>BUILDING</h3>
					<p class="card-desc">Aufbau hochwertiger Backlinks für mehr Autorität und bessere Rankings. Nachhaltiger Linkaufbau nach Google-Richtlinien.</p>
					<span class="sticker-accent sticker-lime-accent">Links</span>
				</div>

				<div class="service-card-new">
					<div class="card-number">06.</div>
					<h3 class="card-title">SEO-MONITORING<br>& REPORTING</h3>
					<p class="card-desc">Kontinuierliche Überwachung Ihrer Rankings und Performance. Transparente Reports mit allen wichtigen Kennzahlen und Entwicklungen.</p>
					<span class="sticker-accent sticker-mint-accent">Monitoring</span>
				</div>
			</div>
			<!-- Lightning Bolt -->
			<img src="/49f6f10d-ca8b-4222-a28f-19e39250e797.jpg" alt="" class="lightning-right" />
		</div>
	</section>

	<!-- Warum SEO -->
	<section class="why-seo">
		<div class="container">
			<div class="why-content">
				<h2 class="why-title">Warum Suchmaschinenoptimierung?</h2>
				<ul class="why-list">
					<li><strong>Mehr Sichtbarkeit:</strong> 90% der Online-Erfahrungen beginnen mit einer Suchmaschine. Seien Sie sichtbar, wenn Ihre Kunden suchen.</li>
					<li><strong>Qualifizierter Traffic:</strong> Erreichen Sie Nutzer mit konkreter Kaufabsicht genau zum richtigen Zeitpunkt.</li>
					<li><strong>Nachhaltige Ergebnisse:</strong> Im Gegensatz zu Werbeanzeigen bringen SEO-Maßnahmen langfristige und nachhaltige Erfolge.</li>
					<li><strong>Messbare Erfolge:</strong> Jede Maßnahme ist nachvollziehbar. Wir zeigen Ihnen transparent, was SEO für Ihr Geschäft bewirkt.</li>
					<li><strong>Bessere Conversion-Rate:</strong> Organischer Traffic konvertiert häufig besser als bezahlte Werbung, da Nutzer gezielt nach Lösungen suchen.</li>
					<li><strong>Wettbewerbsvorteil:</strong> Überholen Sie Ihre Konkurrenz in den Suchergebnissen und gewinnen Sie deren potenzielle Kunden.</li>
				</ul>
			</div>
		</div>
	</section>

	<!-- Blog Preview -->
	<section class="blog-preview">
		<div class="container">
			<h2 class="blog-title">Aktuelles aus unserem Blog</h2>
			<p class="blog-subtitle">Tipps, Trends und Best Practices rund um SEO</p>

			<div class="blog-grid">
				{latestPosts.map((post) => (
					<article class="blog-card">
						<div class="blog-card-tag">{getCategoryEmoji(post.data.category)} {post.data.category}</div>
						<h3 class="blog-card-title">{post.data.title}</h3>
						<p class="blog-card-excerpt">{post.data.description}</p>
						<a href={`/blog/${post.slug}`} class="blog-card-link">Weiterlesen →</a>
					</article>
				))}
			</div>

			<div class="blog-cta">
				<a href="/blog" class="blog-button">
					<span>Alle Artikel ansehen</span>
					<span class="arrow-icon">→</span>
				</a>
			</div>
		</div>
	</section>

	<!-- Kontakt -->
	<section class="contact" id="kontakt">
		<div class="container">
			<div class="contact-content">
				<h2 class="contact-title">Bereit für bessere Rankings?</h2>
				<p class="contact-subtitle">
					Lassen Sie uns gemeinsam Ihre SEO-Strategie entwickeln. Kontaktieren Sie uns für
					eine kostenlose Erstberatung.
				</p>

				<form class="contact-form" name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field" action="/danke">
					<input type="hidden" name="form-name" value="contact" />
					<p style="display: none;">
						<label>Don't fill this out if you're human: <input name="bot-field" /></label>
					</p>
					<div class="form-row">
						<div class="form-group">
							<label for="name">Name *</label>
							<input type="text" id="name" name="name" required />
						</div>
						<div class="form-group">
							<label for="email">E-Mail *</label>
							<input type="email" id="email" name="email" required />
						</div>
					</div>

					<div class="form-group">
						<label for="phone">Telefon</label>
						<input type="tel" id="phone" name="phone" />
					</div>

					<div class="form-group">
						<label for="message">Ihre Nachricht *</label>
						<textarea id="message" name="message" rows="6" required></textarea>
					</div>

					<button type="submit" class="submit-button">
						<span>Nachricht senden</span>
						<span class="arrow">→</span>
					</button>
				</form>

				<div class="contact-info">
					<div class="info-item">
						<span class="info-icon">📍</span>
						<span>Offenbach am Main</span>
					</div>
					<div class="info-item">
						<span class="info-icon">📧</span>
					
					</div>
					<div class="info-item">
						<span class="info-icon">📞</span>
						<span>+49 162 751 9335</span>
					</div>
				</div>
			</div>
		</div>
	</section>

	<!-- Footer -->
	<footer class="footer">
		<div class="container">
			<div class="footer-content">
				<div class="footer-col">
					<h3 class="footer-title">SEO Agentur Offenbach</h3>
					<p class="footer-text">
						Professionelle Suchmaschinenoptimierung für Unternehmen in Offenbach und der Rhein-Main-Region.
					</p>
				</div>
				<div class="footer-col">
					<h4 class="footer-heading">Navigation</h4>
					<ul class="footer-links">
						<li><a href="/">Home</a></li>
						<li><a href="/#leistungen">Leistungen</a></li>
						<li><a href="/blog">Blog</a></li>
						<li><a href="/#kontakt">Kontakt</a></li>
					</ul>
				</div>
				<div class="footer-col">
					<h4 class="footer-heading">Leistungen</h4>
					<ul class="footer-links">
						<li><a href="/#leistungen">SEO Beratung</a></li>
						<li><a href="/#leistungen">Technisches SEO</a></li>
						<li><a href="/#leistungen">Local SEO</a></li>
						<li><a href="/#leistungen">Link Building</a></li>
					</ul>
				</div>
				<div class="footer-col">
					<h4 class="footer-heading">Kontakt</h4>
					<ul class="footer-links">
						<li>📍 Offenbach am Main</li>
						<li>📞 +49 (0) 162 7519335</li>
					</ul>
				</div>
			</div>
			<div class="footer-bottom">
				<p>&copy; 2026 SEO Agentur Offenbach. Alle Rechte vorbehalten. | <a href="/wichtig">Impressum</a> | <a href="/datenschutz">Datenschutz</a></p>
			</div>
		</div>
	</footer>
</BaseLayout>

<style>
	/* Hero Section - Neo-Brutalism */
	.hero {
		min-height: 100vh;
		display: flex;
		align-items: center;
		position: relative;
		overflow: hidden;
		background: var(--dark-petrol);
		background-image:
			radial-gradient(circle at 2px 2px, rgba(255, 255, 255, 0.15) 1px, transparent 0);
		background-size: 40px 40px;
		padding: 8rem 0;
		margin-top: -80px;
		padding-top: calc(8rem + 80px);
	}

	.hero-content {
		text-align: center;
		max-width: 1400px;
		margin: 0 auto;
		position: relative;
		z-index: 2;
	}

	.hero-tag {
		font-family: var(--font-mono);
		font-size: 0.875rem;
		font-weight: 400;
		color: rgba(255, 255, 255, 0.9);
		margin-bottom: 2rem;
		letter-spacing: 0.05em;
	}

	.hero-title {
		font-family: var(--font-display);
		font-size: clamp(2.5rem, 8vw, 5.5rem);
		font-weight: 900;
		line-height: 0.95;
		margin-bottom: 2.5rem;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
	}

	.title-line {
		color: var(--white);
		font-style: italic;
		letter-spacing: -0.03em;
	}

	.sticker-container {
		display: flex;
		gap: 0;
		margin-top: 2rem;
		flex-wrap: wrap;
		justify-content: center;
	}

	.sticker-3d {
		display: inline-block;
		font-size: clamp(2rem, 7vw, 4.5rem);
		font-weight: 900;
		font-style: italic;
		padding: 0.3em 0.8em;
		position: relative;
		letter-spacing: -0.02em;
		transform-style: preserve-3d;
		transition: transform 0.3s ease;
		border-radius: 20px;
	}

	.sticker-mint {
		background: var(--neon-mint);
		color: #0b3d4e;
		transform: rotate(-5deg);
		box-shadow:
			8px 8px 0 rgba(0, 0, 0, 0.2),
			12px 12px 30px rgba(0, 255, 179, 0.4);
		border: 4px solid rgba(0, 0, 0, 0.1);
	}

	.sticker-lime {
		background: var(--neon-lime);
		color: #0b3d4e;
		transform: rotate(3deg);
		box-shadow:
			8px 8px 0 rgba(0, 0, 0, 0.2),
			12px 12px 30px rgba(204, 255, 0, 0.4);
		border: 4px solid rgba(0, 0, 0, 0.1);
		margin-left: -3rem;
	}

	.sticker-3d:hover {
		transform: rotate(0deg) translateY(-5px);
	}

	/* Doodles and Accents */
	.doodles {
		position: absolute;
		width: 100%;
		height: 100%;
		top: 0;
		left: 0;
		pointer-events: none;
		z-index: 1;
	}

	.doodle {
		position: absolute;
		width: 100px;
		height: 100px;
	}

	.arrow-1 {
		top: 20%;
		left: 10%;
		transform: rotate(-20deg);
		animation: float 3s ease-in-out infinite;
	}

	.circle-1 {
		top: 60%;
		right: 15%;
		transform: rotate(15deg);
		animation: float 4s ease-in-out infinite;
	}

	.lightning-icon {
		position: absolute;
		font-size: 4rem;
		top: 30%;
		right: 20%;
		animation: pulse 2s ease-in-out infinite;
		filter: drop-shadow(0 0 20px var(--neon-yellow));
	}

	.paperclip {
		position: absolute;
		font-size: 3rem;
		bottom: 25%;
		left: 15%;
		transform: rotate(-45deg);
		animation: swing 3s ease-in-out infinite;
	}

	@keyframes float {
		0%, 100% { transform: translateY(0) rotate(-20deg); }
		50% { transform: translateY(-20px) rotate(-20deg); }
	}

	@keyframes pulse {
		0%, 100% { opacity: 0.7; transform: scale(1); }
		50% { opacity: 1; transform: scale(1.2); }
	}

	@keyframes swing {
		0%, 100% { transform: rotate(-45deg); }
		50% { transform: rotate(-35deg); }
	}

	/* Hero Footer */
	.hero-footer {
		display: flex;
		flex-direction: column;
		align-items: center;
		margin-top: 1.5rem;
	}

	.cta-button {
		background: var(--white);
		color: #000;
		padding: 1.25rem 3rem;
		border-radius: 100px;
		text-decoration: none;
		font-family: var(--font-display);
		font-weight: 800;
		font-size: 1.125rem;
		display: inline-flex;
		align-items: center;
		gap: 0.875rem;
		transition: all 0.3s ease;
		box-shadow:
			6px 6px 0 rgba(0, 0, 0, 0.2),
			0 10px 40px rgba(255, 255, 255, 0.2);
		border: 3px solid #000;
		position: relative;
	}

	.cta-button:hover {
		transform: translate(-3px, -3px);
		box-shadow:
			9px 9px 0 rgba(0, 0, 0, 0.2),
			0 15px 50px rgba(255, 255, 255, 0.3);
	}

	.pushpin {
		font-size: 1.5rem;
		transform: rotate(45deg);
	}

	.hero-subtext {
		margin-top: 1.5rem;
		color: rgba(255, 255, 255, 0.85);
		font-family: var(--font-mono);
		font-size: 0.875rem;
		letter-spacing: 0.05em;
		text-transform: lowercase;
	}

	/* Services Grid Section */
	.services-grid-section {
		padding: 6rem 0;
		background: var(--dark-petrol);
		background-image:
			radial-gradient(circle at 2px 2px, rgba(255, 255, 255, 0.15) 1px, transparent 0);
		background-size: 40px 40px;
		position: relative;
		overflow: hidden;
	}

	.section-title-new {
		font-family: var(--font-display);
		font-size: 3.5rem;
		font-weight: 900;
		text-align: center;
		margin-bottom: 4rem;
		color: #ffffff;
		letter-spacing: -0.02em;
	}

	.services-grid-new {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2.5rem;
		max-width: 1400px;
		margin: 0 auto;
		position: relative;
		z-index: 2;
	}

	.service-card-new {
		background: #ffffff;
		padding: 3rem;
		border-radius: 20px;
		position: relative;
		transition: transform 0.3s ease;
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
		transform: rotate(-1deg);
	}

	.service-card-new:nth-child(even) {
		transform: rotate(1deg);
	}

	.service-card-new:hover {
		transform: translateY(-10px) rotate(0deg);
	}

	.paperclip-icon {
		position: absolute;
		top: -10px;
		right: 30px;
		font-size: 3rem;
		opacity: 0.6;
		transform: rotate(45deg);
	}

	.card-number {
		font-family: var(--font-display);
		font-size: 3rem;
		font-weight: 900;
		color: #000;
		line-height: 1;
		margin-bottom: 1rem;
	}

	.card-title {
		font-family: var(--font-display);
		font-size: 2rem;
		font-weight: 900;
		color: #000;
		line-height: 1.1;
		margin-bottom: 1.5rem;
		letter-spacing: -0.02em;
	}

	.card-desc {
		font-size: 1rem;
		line-height: 1.6;
		color: #333;
		margin-bottom: 2rem;
	}

	.sticker-accent {
		display: inline-block;
		padding: 0.5rem 1.5rem;
		font-family: var(--font-display);
		font-weight: 800;
		font-size: 0.875rem;
		color: #000;
		border: 2px solid #000;
		border-radius: 100px;
		position: absolute;
		bottom: -15px;
		right: 30px;
		letter-spacing: 0.05em;
		text-transform: uppercase;
	}

	.sticker-lime-accent {
		background: var(--neon-lime);
		transform: rotate(-3deg);
		box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.2);
	}

	.sticker-mint-accent {
		background: var(--neon-mint);
		transform: rotate(2deg);
		box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.2);
	}

	/* Lightning Bolts */
	.lightning-left,
	.lightning-right,
	.lightning-top {
		position: absolute;
		width: 80px;
		height: auto;
		opacity: 0.4;
		z-index: 1;
		filter: drop-shadow(0 0 15px rgba(223, 255, 0, 0.5));
	}

	.lightning-left {
		left: 5%;
		bottom: 15%;
		transform: rotate(-20deg);
		animation: pulse 3s ease-in-out infinite;
	}

	.lightning-right {
		right: 8%;
		top: 50%;
		transform: rotate(25deg);
		animation: pulse 3s ease-in-out infinite 1s;
		width: 100px;
	}

	.lightning-top {
		right: 10%;
		top: 10%;
		transform: rotate(-15deg);
		animation: pulse 3s ease-in-out infinite 2s;
		width: 90px;
	}

	/* About */
	.about {
		padding: 7rem 0;
		background: #ffffff;
	}

	.about-content {
		max-width: 1000px;
		margin: 0 auto;
		text-align: left;
	}

	.about-title {
		font-family: var(--font-display);
		font-size: 4rem;
		font-weight: 900;
		color: #000;
		margin-bottom: 1rem;
		line-height: 1.2;
		letter-spacing: -0.02em;
	}

	.about-subtitle {
		font-family: var(--font-display);
		font-size: 1.875rem;
		font-weight: 700;
		line-height: 1.3;
		color: #000;
		margin-bottom: 2rem;
	}

	.about-description {
		font-size: 1.125rem;
		line-height: 1.8;
		color: #333;
		margin-bottom: 0;
	}

	/* Services */
	.services {
		padding: 5rem 0;
		background: #f5f5f5;
	}

	.section-title {
		font-size: 3.5rem;
		text-align: center;
		margin-bottom: 4rem;
		font-weight: 800;
		color: #333;
	}

	.services-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
		gap: 2rem;
	}

	.service-card {
		transition: transform 0.3s, box-shadow 0.3s;
	}

	.service-card:hover {
		transform: translateY(-8px) rotate(1deg);
	}

	.service-icon {
		font-size: 3.5rem;
		margin-bottom: 1.25rem;
	}

	.service-card h3 {
		font-size: 1.5rem;
		margin-bottom: 1rem;
		color: #333;
	}

	.service-card p {
		line-height: 1.7;
		color: #666;
	}

	.card-neon-green h3,
	.card-neon-cyan h3,
	.card-neon-yellow h3 {
		color: #000000;
	}

	.card-neon-green p,
	.card-neon-cyan p,
	.card-neon-yellow p {
		color: #000000;
		opacity: 0.85;
	}

	/* Why SEO */
	.why-seo {
		padding: 7rem 0;
		background: #ffffff;
	}

	.why-content {
		max-width: 1000px;
		margin: 0 auto;
		text-align: left;
	}

	.why-title {
		font-family: var(--font-display);
		font-size: 4rem;
		font-weight: 900;
		color: #000;
		margin-bottom: 2rem;
		line-height: 1.2;
		letter-spacing: -0.02em;
	}

	.why-list {
		list-style: none;
		padding: 0;
		margin: 0;
	}

	.why-list li {
		font-size: 1.125rem;
		line-height: 1.8;
		color: #333;
		margin-bottom: 1.5rem;
		padding-left: 2rem;
		position: relative;
		font-weight: 500;
	}

	.why-list li::before {
		content: "→";
		position: absolute;
		left: 0;
		color: #000;
		font-weight: 900;
		font-size: 1.25rem;
	}

	.why-list li strong {
		font-family: var(--font-display);
		font-weight: 700;
		color: #000;
	}

	/* Blog Preview */
	.blog-preview {
		padding: 7rem 0;
		background: #f5f5f5;
	}

	.blog-title {
		font-family: var(--font-display);
		font-size: 4rem;
		font-weight: 900;
		color: #000;
		text-align: center;
		margin-bottom: 1rem;
		line-height: 1.2;
		letter-spacing: -0.02em;
	}

	.blog-subtitle {
		font-family: var(--font-display);
		font-size: 1.5rem;
		font-weight: 700;
		color: #000;
		text-align: center;
		margin-bottom: 4rem;
		opacity: 0.7;
	}

	.blog-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2.5rem;
		max-width: 1400px;
		margin: 0 auto 4rem;
	}

	.blog-card {
		background: #ffffff;
		padding: 2.5rem;
		border-radius: 15px;
		border: 3px solid #000;
		box-shadow: 8px 8px 0 rgba(0, 0, 0, 0.2);
		transition: transform 0.3s ease;
		position: relative;
	}

	.blog-card:hover {
		transform: translateY(-5px);
		box-shadow: 12px 12px 0 rgba(0, 0, 0, 0.2);
	}

	.blog-card-tag {
		display: inline-block;
		background: var(--neon-lime);
		color: #000;
		font-family: var(--font-display);
		font-weight: 800;
		font-size: 0.875rem;
		padding: 0.5rem 1rem;
		border-radius: 50px;
		border: 2px solid #000;
		margin-bottom: 1.5rem;
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	.blog-card-title {
		font-family: var(--font-display);
		font-size: 1.5rem;
		font-weight: 900;
		color: #000;
		line-height: 1.3;
		margin-bottom: 1rem;
		letter-spacing: -0.01em;
	}

	.blog-card-excerpt {
		font-size: 1rem;
		line-height: 1.6;
		color: #333;
		margin-bottom: 1.5rem;
	}

	.blog-card-link {
		font-family: var(--font-display);
		font-weight: 700;
		font-size: 1rem;
		color: #000;
		text-decoration: none;
		display: inline-block;
		transition: transform 0.2s ease;
	}

	.blog-card-link:hover {
		transform: translateX(5px);
	}

	.blog-cta {
		text-align: center;
		margin-top: 3rem;
	}

	.blog-button {
		display: inline-flex;
		align-items: center;
		gap: 1rem;
		background: var(--neon-mint);
		color: #000;
		font-family: var(--font-display);
		font-weight: 800;
		font-size: 1.125rem;
		padding: 1.25rem 2.5rem;
		border-radius: 100px;
		border: 3px solid #000;
		text-decoration: none;
		box-shadow: 6px 6px 0 rgba(0, 0, 0, 0.2);
		transition: all 0.3s ease;
	}

	.blog-button:hover {
		transform: translateY(-3px);
		box-shadow: 8px 8px 0 rgba(0, 0, 0, 0.2);
	}

	.arrow-icon {
		font-size: 1.5rem;
		transition: transform 0.3s ease;
	}

	.blog-button:hover .arrow-icon {
		transform: translateX(5px);
	}

	.neon-button:hover {
		transform: translateY(-3px);
		box-shadow: 0 0 40px rgba(223, 255, 0, 0.7);
	}

	/* Contact */
	.contact {
		padding: 7rem 0 0;
		background: var(--dark-petrol);
		background-image:
			radial-gradient(circle at 2px 2px, rgba(255, 255, 255, 0.15) 1px, transparent 0);
		background-size: 40px 40px;
	}

	.contact-content {
		max-width: 900px;
		margin: 0 auto;
	}

	.contact-title {
		font-family: var(--font-display);
		font-size: 4rem;
		font-weight: 900;
		color: #ffffff;
		text-align: center;
		margin-bottom: 1.5rem;
		line-height: 1.2;
		letter-spacing: -0.02em;
	}

	.contact-subtitle {
		font-size: 1.25rem;
		color: rgba(255, 255, 255, 0.9);
		text-align: center;
		margin-bottom: 4rem;
		font-weight: 500;
	}

	.contact-form {
		background: #ffffff;
		padding: 3rem;
		border-radius: 20px;
		border: 3px solid #000;
		box-shadow: 12px 12px 0 rgba(0, 0, 0, 0.3);
		margin-bottom: 3rem;
	}

	.form-row {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 1.5rem;
		margin-bottom: 1.5rem;
	}

	.form-group {
		margin-bottom: 1.5rem;
	}

	.form-group label {
		display: block;
		font-family: var(--font-display);
		font-weight: 700;
		font-size: 0.875rem;
		color: #000;
		margin-bottom: 0.5rem;
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	.form-group input,
	.form-group textarea {
		width: 100%;
		padding: 1rem;
		font-family: inherit;
		font-size: 1rem;
		border: 2px solid #000;
		border-radius: 10px;
		background: #ffffff;
		color: #000;
		transition: all 0.3s ease;
	}

	.form-group input:focus,
	.form-group textarea:focus {
		outline: none;
		border-color: var(--neon-lime);
		box-shadow: 0 0 0 3px rgba(204, 255, 0, 0.2);
	}

	.form-group textarea {
		resize: vertical;
		font-family: inherit;
	}

	.submit-button {
		display: inline-flex;
		align-items: center;
		gap: 1rem;
		background: var(--neon-lime);
		color: #000;
		font-family: var(--font-display);
		font-weight: 800;
		font-size: 1.125rem;
		padding: 1.25rem 2.5rem;
		border-radius: 100px;
		border: 3px solid #000;
		cursor: pointer;
		box-shadow: 6px 6px 0 rgba(0, 0, 0, 0.2);
		transition: all 0.3s ease;
	}

	.submit-button:hover {
		transform: translateY(-3px);
		box-shadow: 8px 8px 0 rgba(0, 0, 0, 0.2);
	}

	.submit-button .arrow {
		font-size: 1.5rem;
		transition: transform 0.3s ease;
	}

	.submit-button:hover .arrow {
		transform: translateX(5px);
	}

	.contact-info {
		display: flex;
		justify-content: center;
		gap: 3rem;
		flex-wrap: wrap;
	}

	.info-item {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		color: rgba(255, 255, 255, 0.9);
		font-family: var(--font-mono);
		font-size: 0.875rem;
		font-weight: 700;
	}

	.info-icon {
		font-size: 1.25rem;
	}

	/* Footer */
	.footer {
		background: var(--dark-petrol);
		background-image:
			radial-gradient(circle at 2px 2px, rgba(255, 255, 255, 0.15) 1px, transparent 0);
		background-size: 40px 40px;
		padding: 5rem 0 2rem;
		border-top: 3px solid rgba(255, 255, 255, 0.1);
	}

	.footer-content {
		display: grid;
		grid-template-columns: repeat(4, 1fr);
		gap: 3rem;
		margin-bottom: 3rem;
	}

	.footer-col {
		color: rgba(255, 255, 255, 0.9);
	}

	.footer-title {
		font-family: var(--font-display);
		font-size: 1.5rem;
		font-weight: 900;
		color: #ffffff;
		margin-bottom: 1rem;
		letter-spacing: -0.01em;
	}

	.footer-text {
		font-size: 0.9375rem;
		line-height: 1.6;
		color: rgba(255, 255, 255, 0.8);
	}

	.footer-heading {
		font-family: var(--font-display);
		font-weight: 800;
		font-size: 1rem;
		color: #ffffff;
		margin-bottom: 1rem;
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	.footer-links {
		list-style: none;
		padding: 0;
		margin: 0;
	}

	.footer-links li {
		margin-bottom: 0.75rem;
		font-size: 0.9375rem;
	}

	.footer-links a {
		color: rgba(255, 255, 255, 0.8);
		text-decoration: none;
		transition: color 0.3s ease;
	}

	.footer-links a:hover {
		color: var(--neon-lime);
	}

	.footer-bottom {
		padding-top: 2rem;
		border-top: 1px solid rgba(255, 255, 255, 0.1);
		text-align: center;
	}

	.footer-bottom p {
		color: rgba(255, 255, 255, 0.6);
		font-size: 0.875rem;
		margin: 0;
	}

	.footer-bottom a {
		color: rgba(255, 255, 255, 0.7);
		text-decoration: none;
		transition: color 0.3s ease;
	}

	.footer-bottom a:hover {
		color: var(--neon-lime);
	}

	/* Responsive */
	@media (max-width: 1024px) {
		.hero {
			padding: 6rem 0;
		}

		.doodles .lightning-icon,
		.doodles .paperclip {
			display: none;
		}

		.services-grid-new {
			grid-template-columns: repeat(2, 1fr);
		}

		.section-title-new {
			font-size: 2.5rem;
		}

		.blog-grid {
			grid-template-columns: repeat(2, 1fr);
		}
	}

	@media (max-width: 768px) {
		.container {
			padding: 0 1rem;
		}

		.hero {
			padding: 4rem 0;
			min-height: auto;
		}

		.hero-title {
			margin-top: 3rem;
		}

		.hero-tag {
			font-size: 0.875rem;
			margin-bottom: 2rem;
		}

		.sticker-container {
			gap: 1rem;
		}

		.sticker-3d {
			font-size: clamp(2rem, 8vw, 4rem);
		}

		.doodles .arrow-1,
		.doodles .circle-1 {
			display: none;
		}

		.hero-footer {
			margin-top: 2rem;
		}

		.cta-button {
			padding: 1.25rem 2.5rem;
			font-size: 1rem;
		}

		.section-title {
			font-size: 2.5rem;
		}

		.services-grid,
		.why-grid {
			grid-template-columns: 1fr;
		}

		.service-card,
		.why-card,
		.post-card,
		.contact-form {
			box-shadow: 6px 6px 0 rgba(0, 0, 0, 0.2);
		}

		.contact-title {
			font-size: 2.5rem;
		}

		.contact-subtitle {
			font-size: 1rem;
		}

		.contact-form {
			padding: 2rem;
		}

		.form-row {
			grid-template-columns: 1fr;
			gap: 0;
		}

		.submit-button {
			width: 100%;
			justify-content: center;
		}

		.contact-info {
			flex-direction: column;
			gap: 1.5rem;
		}

		.footer-content {
			grid-template-columns: 1fr;
			gap: 2.5rem;
		}

		.services-grid-new {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.service-card-new {
			padding: 2rem;
		}

		.card-number {
			font-size: 2rem;
		}

		.card-title {
			font-size: 1.5rem;
		}

		.sticker-accent {
			bottom: -12px;
			right: 20px;
			padding: 0.4rem 1rem;
			font-size: 0.75rem;
		}

		.blog-grid {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.blog-title {
			font-size: 2.5rem;
		}

		.blog-subtitle {
			font-size: 1.125rem;
		}

		.blog-card {
			padding: 2rem;
		}

		.blog-button {
			padding: 1rem 2rem;
			font-size: 1rem;
		}
	}
</style>
</BaseLayout>
